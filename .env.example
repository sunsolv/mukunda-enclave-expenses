# Frontend-safe build configuration (never add the service-role key here)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-publishable-anon-key
APP_ORIGIN=http://localhost:4200
PRODUCTION_BASE_URL=https://sunsolv.github.io/mukunda-enclave-expenses/
GITHUB_PAGES_BASE_PATH=/mukunda-enclave-expenses/

# Server-side provisioning/import only. Keep in a secure shell or secret store.
# SUPABASE_SERVICE_ROLE_KEY=never-commit-this
# INITIAL_TEMP_PASSWORD=provide-interactively
# IMPORT_CREATED_BY=emergency-admin-profile-uuid
